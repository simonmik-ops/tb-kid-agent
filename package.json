{
  "name": "tb-kid-agent",
  "version": "1.6.0",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/run-tests.js",
    "visual:compare": "node scripts/visual-regression.js",
    "catalog:build": "node scripts/generate-format-catalog.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "multer": "^2.1.0",
    "node-fetch": "^2.7.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "pixelmatch": "^5.3.0",
    "pngjs": "^7.0.0"
  }
}
